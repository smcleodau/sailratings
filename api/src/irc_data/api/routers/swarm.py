"""Swarm router — placeholder.

The /v1/swarm endpoints are not yet implemented. This stub keeps
``irc_data.api.app`` importable so the API can boot; replace it with
real routes when the swarm feature is built out.
"""

from fastapi import APIRouter

router = APIRouter()
